
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  "appId": "com.karbonsites.gesture3drunner",
  "appName": "Gesture 3D Runner",
  "webDir": "www",
  "server": {
    "androidScheme": "https"
  }
};

export default config;
    